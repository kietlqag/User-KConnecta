spring:
  data:
    redis:
      host: redis
      port: 6379
      timeout: 2000

  cache:
    type: redis
    cache-names: userById, userByUsername
    redis:
      time-to-live: 600000    # 10 minutes in ms
      cache-null-values: false
